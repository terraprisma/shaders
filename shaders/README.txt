Shader `.cso` files are extracted from their respective `.fnb`s.
Shader `.asm` files are disassembled by `ShaderDisassembler.
New shaders are compiled with `fxc.exe`.
New shaders are disassembled by `ShaderDisassembler` after compilation.